# ── Core web server ─────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
sse-starlette>=1.6.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── Vision & pose detection ──────────────────────────────────────────────────
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0

# ── LangChain core ──────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0

# ── LLM integrations ────────────────────────────────────────────────────────
langchain-google-genai>=1.0.0      # Gemini Flash (real-time path)
langchain-community>=0.2.0         # Ollama / Qwen (local private path)

# ── Supabase (pgvector RAG) ──────────────────────────────────────────────────
supabase>=2.0.0

# ── Google AI (embeddings for pgvector) ─────────────────────────────────────
google-generativeai>=0.5.0
